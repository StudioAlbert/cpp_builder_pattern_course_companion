# Completed solution (also the `solution/sandwich-shop` branch).
# src/main.cc asserts the expected Content() output.
add_executable(sandwich_shop_solution src/main.cc)
target_include_directories(sandwich_shop_solution PRIVATE include)
